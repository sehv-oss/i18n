name: Release

on:
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  release:
    uses: sehv-oss/actions/.github/workflows/release-node-pnpm.yaml@main
    secrets: inherit
